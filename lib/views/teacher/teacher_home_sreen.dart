import 'package:flutter/material.dart';

class TeacherHomeSreen extends StatelessWidget {
  const TeacherHomeSreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Teacher Home Screen'),
      ),
    );
  }
}
